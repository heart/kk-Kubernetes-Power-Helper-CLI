# kk – Kubernetes Power Helper CLI

_วิธีใช้ Kubernetes ที่เร็วขึ้น ชัดขึ้น และลดงานซ้ำซ้อนแบบรู้สึกได้ทันที_

![kk logo](logo.png)

---

## ทำไมต้องมี kk

ใช้ `kubectl` ตรง ๆ มักตามมาด้วย:

- คำสั่งยาว ๆ ที่ต้องพิมพ์ซ้ำ
- ใส่ `-n namespace` เต็มไปหมด
- ต้องหา pod name ทุกครั้ง
- ก็อป/วางชื่อ pod ที่ยาวและเปลี่ยนเรื่อย ๆ
- ดีบั๊กหลาย replica แล้วงงไปหมด

`kk` ถูกสร้างขึ้นเพื่อแก้ “ความน่ารำคาญ” เหล่านี้โดยเฉพาะ  
ไม่ต้องติดตั้งฝั่ง cluster  
ไม่มี CRD  
เป็นแค่สคริปต์ Bash บนเครื่องคุณ  
แต่ช่วยให้ **ทำงานเร็วขึ้น ใช้ง่ายขึ้น และดีบั๊กไวขึ้นมาก**

---

## จุดแข็งของ kk

### 🔹 1. Namespace จำค่าให้เอง

ตั้งครั้งเดียว:

```
kk ns set staging
```

หลังจากนั้นทุกคำสั่งจะใช้ namespace นี้อัตโนมัติ  
ไม่ต้องพิมพ์ `-n staging` ทุกครั้งอีกต่อไป

---

### 🔹 2. Pattern-first Pod Selection

**เลิกล่า pod name — ใช้แค่คำที่ “สื่อเจตนา” ของคุณก็พอ**

ใน cluster จริง pod จะชื่อประมาณนี้:

```
api-server-7f9c8d7c9b-xyz12
api-server-7f9c8d7c9b-a1b2c
api-worker-64c8b54fd9-jkq8n
```

ปกติคุณต้อง:

- `kubectl get pods`
- หา pod ที่ต้องการ
- ก็อปชื่อยาว ๆ
- วาง ๆ แล้วพิมพ์ต่อ
- พอ restart ก็ต้องทำใหม่

kk ตัดขั้นตอนทั้งหมดนี้ทิ้งไป

#### ⭐ Pattern-first คืออะไร

ใช้ substring หรือ regex อะไรก็ได้เพื่อเลือก pod/deployment

```
kk logs api
kk sh api
kk desc api
```

เลือกเป็นกลุ่ม:

```
kk logs server
kk logs worker
kk restart '^api-server'
```

เลือกเฉพาะจุดเฉพาะงาน:

```
kk sh 'order.*prod'
```

ถ้ามีหลายตัวให้เลือก kk จะเปิด `fzf` หรือเลือกแบบลำดับเลข  
ลดความผิดพลาดทั้งทางสายตาและความจำ

#### ⭐ ทำไมสำคัญ

เพราะมันช่วยให้คุณไม่ต้อง:

- เปิดรายชื่อ pod ยาว ๆ
- ก็อปชื่อ pod ที่ท้ายเป็น hash
- เจอปัญหา pod restart แล้วชื่อเปลี่ยน
- พิมพ์ผิดจาก suffix ยาว ๆ

คุณพิมพ์ “เจตนา” ของคุณ  
และ kk จะหาตัวที่ถูกให้เอง

#### ⭐ ใช้ร่วมกับทุกอย่างใน kk

หนึ่ง pattern ใช้ได้ทุกคำสั่ง:

```
kk pods api
kk svc api
kk desc api
kk images api
kk restart api
```

---

### 🔹 3. Multi-pod Log Streaming & Debugging แบบใช้งานได้จริง

ดีบั๊ก Kubernetes ยุ่งยากเพราะระบบกระจายตัว replica เยอะ  
ต้องเช็ค pod ไหน?  
บรรทัดนี้มาจากตัวไหน?  
บางครั้งพฤติกรรมขัดกันระหว่าง replica

`kk` ทำให้สิ่งเหล่านี้ “เรียบง่าย”:

```
kk logs api -g "traceId=123"
```

สิ่งที่เกิดขึ้น:

- เลือกทุก pod ที่ชื่อมีคำว่า `api`
- ดึง log จากทุก replica พร้อมกัน
- กรองเฉพาะบรรทัดที่มี `traceId=123`
- ทุกบรรทัด prefix ด้วยชื่อ pod
- เห็นทันทีว่า log มาจาก replica ไหน

ทำให้ปัญหาแบบ:

- request ขาด ๆ หาย ๆ
- พฤติกรรมไม่เหมือนกันในแต่ละ replica
- trace กระโดดข้าม pod

กลายเป็นเรื่องไล่ได้ง่ายขึ้นมาก  
คุณไม่ต้อง “ล่า log” — คุณเพียง “ตามร่องรอย”

---

### 🔹 4. ตัวช่วยดีบั๊กที่จำเป็นจริง ๆ

- `kk top api` – ลูกเล่นกรอง CPU/Memory
- `kk desc api` – describe ผ่าน pattern
- `kk events` – event ล่าสุดใน namespace
- `kk pf api 8080:80` – port-forward ที่มี guard rails
- `kk images api` – ดึง image ด้วย `jq`

kk เติมเต็ม workflow ที่ dev ทำทุกวัน

---

## ก่อนใช้ kk / หลังใช้ kk

### ก่อนใช้ kk:

```
kubectl get pods -n staging | grep api
kubectl logs api-7f9c9d7c9b-xyz -n staging -f | grep ERROR
kubectl exec -it api-7f9c9d7c9b-xyz -n staging -- /bin/bash
```

### หลังใช้ kk:

```
kk pods api
kk logs api -f -g ERROR
kk sh api
```

**เหมือนเดิมทุกอย่าง—แต่เร็วกว่า คล่องกว่า ฉลาดกว่า**

---

## คำสั่งที่เรียกใช้ได้

| คำสั่ง        | รูปแบบ (Syntax)                                                                       | คำอธิบาย                                                                                                                                                                                                                                                                                                             |
| ---           | ---                                                                                    | ---                                                                                                                                                                                                                                                                                                                   |
| `ns`          | `kk ns [show \| set <namespace> \| list]`                                             | จัดการ namespace เริ่มต้นที่ kk ใช้สำหรับทุกคำสั่ง เก็บค่าไว้ในไฟล์ `~/.kk` เมื่อใช้ `show` จะแสดง namespace ปัจจุบัน, `set` เปลี่ยนค่า namespace, ส่วน `list` ให้เลือก namespace จาก cluster (ใช้ `fzf` ถ้ามีติดตั้ง).                                                                                              |
| `pods`        | `kk pods [pattern]`                                                                   | แสดงรายการ pods ใน namespace ปัจจุบัน ถ้าระบุ `pattern` จะใช้เป็น regex กรองเฉพาะ pod ที่ชื่อ match พร้อมคง header แถวแรกไว้เสมอ.                                                                                                                                                                                    |
| `svc`         | `kk svc [pattern]`                                                                    | แสดงรายการ services ใน namespace ปัจจุบัน ถ้ามี `pattern` จะใช้เป็น regex เพื่อกรองชื่อ service โดยยังคง header แถวแรกไว้.                                                                                                                                                                                           |
| `sh`, `shell` | `kk sh <pod-pattern> [-- COMMAND ...]`                                                | เข้า shell (หรือคำสั่งอื่น) ใน pod ที่เลือกจาก regex `pod-pattern` ถ้าตรงหลายตัวจะให้เลือกผ่าน `fzf` หรือเมนูลำดับเลข จากนั้นเรียก `kubectl exec -ti` เข้า pod นั้น ถ้าไม่ระบุคำสั่ง จะใช้ `/bin/sh` เป็นค่าปริยาย.                                                                                                  |
| `logs`        | `kk logs <pod-pattern> [-c container] [-g pattern] [-f] [-- extra kubectl logs args]` | ดึง log จาก **ทุก pod** ที่ชื่อ match กับ `pod-pattern` อัตโนมัติ สามารถใช้ `-c/--container` ระบุ container, ใช้ `-f/--follow` เพื่อ tail log และ `-g/--grep` เพื่อกรองบรรทัดด้วย regex หลังจาก prefix แต่ละบรรทัดด้วย `[pod-name]` แล้ว argument หลัง `--` จะถูกส่งต่อให้ `kubectl logs` ตรง ๆ (เช่น `--since=5m`). |
| `images`      | `kk images <pod-pattern>`                                                             | แสดงชื่อ container และ image ของทุก pod ที่ชื่อ match กับ `pod-pattern` ต้องมี `jq` ติดตั้งก่อน ใช้สำหรับดูภาพรวมว่า pod นั้น ๆ รัน image อะไรอยู่บ้าง.                                                                                                                                                              |
| `restart`     | `kk restart <deploy-pattern>`                                                         | สั่ง rollout restart deployment โดยใช้ regex `deploy-pattern` เพื่อค้นหา deployment ถ้ามีหลายตัวให้เลือกผ่าน `fzf` หรือเมนูลำดับเลข แล้วรัน `kubectl rollout restart deploy/<name>` ใน namespace ปัจจุบัน.                                                                                                           |
| `pf`          | `kk pf <pod-pattern> <local:remote> [extra args]`                                     | ทำ port-forward ไปยัง pod ที่เลือกด้วย regex `pod-pattern` เลือกให้เหลือเพียงหนึ่ง pod แล้วเรียก `kubectl port-forward` ด้วย mapping `local:remote` ตามที่ระบุ และสามารถส่ง argument เพิ่มเติมให้ `kubectl` ได้ มีข้อความอธิบายเมื่อ port-forward ล้มเหลว (เช่น port ซ้ำ หรือ pod กำลัง restart).                    |
| `desc`        | `kk desc <pod-pattern>`                                                               | เรียก `kubectl describe pod` กับ pod ที่ชื่อ match `pod-pattern` โดยใช้กลไกเลือก pod แบบ regex เดียวกับคำสั่งอื่น.                                                                                                                                                                                                   |
| `top`         | `kk top [pattern]`                                                                    | แสดงการใช้ CPU/Memory ของ pods ใน namespace ปัจจุบันด้วย `kubectl top pod` ถ้าระบุ `pattern` จะใช้เป็น regex กรองชื่อ pod โดยยังคง header แถวแรกไว้.                                                                                                                                                                 |
| `events`      | `kk events`                                                                           | แสดง events ล่าสุดใน namespace ปัจจุบัน พยายาม sort ตาม `.lastTimestamp` และถ้าไม่ได้จะ fallback ไปใช้ `.metadata.creationTimestamp` ใช้ดู error, restart และเหตุการณ์ผิดปกติได้เร็ว.                                                                                                                                |
| `deploys`     | `kk deploys`                                                                          | สรุปข้อมูล deployments ใน namespace ปัจจุบัน ถ้ามี `jq` จะพิมพ์ตารางที่อ่านง่ายพร้อม `NAME`, จำนวน `READY/desired` และ image แรกของ container ถ้าไม่มี `jq` จะ fallback ไปใช้ `kubectl get deploy` ตรง ๆ.                                                                                                            |
| `ctx`         | `kk ctx [list \| use \| show] [...]`                                                  | จัดการ context ของ `kubectl` — `list` (หรือไม่ใส่อะไรเลย) แสดง contexts ทั้งหมด, `use <name>` สลับ context, และ `show [name]` แสดงรายละเอียดของ context (ค่าเริ่มต้นคือ context ปัจจุบัน). |
| `help`        | `kk help` / `kk -h` / `kk --help`                                                     | แสดงหน้าช่วยเหลือ usage ของ kk รวมสรุปทุก subcommand, argument ที่ใช้ได้ และหมายเหตุเรื่อง namespace กับการใช้ regex เป็น pattern เลือก resource.                                                                                                                                                                    |

### คำสั่งลัด (Shortcuts)

`kk` รองรับคำย่อสไตล์ kubectl เพื่อความรวดเร็ว:

| คำย่อ                               | เทียบเท่ากับ |
| ---                                 | ---          |
| `po`, `pod`                         | `pods`       |
| `svc`, `service`, `services`        | `svc`        |
| `exec`, `shell`                     | `sh`         |
| `log`                               | `logs`       |
| `img`                               | `images`     |
| `rollout`                           | `restart`    |
| `pf`, `port-forward`, `portforward` | `pf`         |
| `describe`                          | `desc`       |
| `usage`, `resources`                | `top`        |
| `event`                             | `events`     |
| `deploy`, `deployments`             | `deploys`    |
| `context`, `contexts`               | `ctx`        |
| `namespace`                         | `ns`         |

---

## เปรียบเทียบ kk vs raw kubectl

| งานที่ต้องทำ                | kubectl                                              | kk                                          |
| --------------------------- | ---------------------------------------------------- | ------------------------------------------- |
| ดู namespace ปัจจุบัน       | `kubectl config view --minify` (หรือคำสั่งใกล้เคียง) | `kk ns show`                                |
| ดูรายการ pods               | `kubectl get pods -n <ns>`                           | `kk pods`                                   |
| กรอง pods ตามชื่อ           | `kubectl get pods -n <ns> (แล้ว grep api)`           | `kk pods api`                               |
| ดู log ของ pod เดียว        | `kubectl logs -f pod-xyz -n <ns>`                    | `kk logs pod-xyz -f`                        |
| ดู log ของหลาย pod พร้อมกัน | loop / xargs / เครื่องมืออย่าง `stern`               | `kk logs api -f` (ดึงทุก pod ที่ชื่อ match) |
| exec เข้า pod               | `kubectl exec -ti pod-xyz -n <ns> -- /bin/sh`        | `kk sh pod-xyz`                             |
| ดู service                  | `kubectl get svc -n <ns>`                            | `kk svc`                                    |
| restart deployment          | `kubectl rollout restart deploy/api -n <ns>`         | `kk restart api`                            |
| สรุปรายละเอียด deployments  | `kubectl get deploy -n <ns> -o json + jq ...`        | `kk deploys`                                |
| ดู contexts                 | `kubectl config get-contexts`                        | `kk ctx list` / `kk ctx`                    |
| เปลี่ยน context             | `kubectl config use-context myctx`                   | `kk ctx use myctx` / `kk ctx myctx`         |

---

## การติดตั้ง

```
git clone git@github.com:heart/kk-Kubernetes-Power-Helper-CLI.git
cd kk-Kubernetes-Power-Helper-CLI
cp kk.sh ~/.kk.sh
echo 'source ~/.kk.sh' >> ~/.bashrc   # หรือไฟล์ rc ของ shell ที่คุณใช้จริง
# เปิด shell ใหม่ (หรือ `source ~/.bashrc`) แล้วเริ่มใช้ kk
```

หรือแบบ one-line:

```
curl -fsSL https://raw.githubusercontent.com/heart/kk-Kubernetes-Power-Helper-CLI/main/install-kk.sh | sudo bash
# จากนั้น reload shell เพื่อให้ kk() จาก /etc/profile.d/kk.sh ถูกโหลด
# ถ้าใช้ macOS สคริปต์จะเตือนให้เพิ่มคำสั่ง:  source /usr/local/lib/kk.sh  ลงใน ~/.zshrc หรือไฟล์ rc อื่น
```

### การถอนการติดตั้ง

```
curl -fsSL https://raw.githubusercontent.com/heart/kk-Kubernetes-Power-Helper-CLI/main/uninstall-kk.sh | sudo bash
```

หรือถ้าคุณติดตั้งแบบ manual (เช่น source `~/.kk.sh`) ก็แค่ลบไฟล์นั้นและนำบรรทัด `source ~/.kk.sh` ออกจากไฟล์ rc ของ shell ที่คุณใช้

---

## เริ่มต้นใช้งาน

```
kk ns set my-namespace
kk pods
kk logs api -f -g ERROR
kk sh api -- /bin/bash
```

---

## ปรัชญาของโปรเจกต์

1. **เรียบง่าย** – สคริปต์ Bash ไฟล์เดียว
2. **ลดงานซ้ำ** – ให้เครื่องทำแทน
3. **ไม่ปิดบัง Kubernetes** – คำสั่งคุ้นเคยเหมือนเดิม
4. **ปลอดภัย** – เตือนก่อนทำสิ่งที่กระทบระบบ
5. **สไตล์ Unix** – pipe-friendly, grep-friendly

---

## การมีส่วนร่วม

ยินดีรับ Pull Request  
ขอเพียงเขียนให้ชัดเจน อ่านง่าย และสอดคล้องปรัชญาของโปรเจกต์

Happy troubleshooting!
